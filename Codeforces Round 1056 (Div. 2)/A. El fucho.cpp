#include <bits/stdc++.h>
using namespace std;

int main() {
    ios::sync_with_stdio(false);
    cin.tie(nullptr);
    cout.tie(nullptr);
    int tt;
    cin >> tt;
    while (tt--) {
        long long n;
        cin>>n;
        int total_matches = 2*n - 2;
        cout<<total_matches<<"\n";
    }

    
    return 0;
}