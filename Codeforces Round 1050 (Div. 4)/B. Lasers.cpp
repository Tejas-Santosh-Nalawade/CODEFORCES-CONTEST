#include <bits/stdc++.h>
using namespace std;

int main() {
    ios::sync_with_stdio(false);
    cin.tie(nullptr);
    cout.tie(nullptr);
    int tt;
    cin >> tt;
    while (tt--) {
        long long n,m,x,y;
        cin>>n>>m>>x>>y;
        for(int i=0;i<n;i++){
            long long a;
            cin>>a;
        } 
        for(int i=0;i<m;i++){
            long long b;
            cin>>b;
        } 
        cout<<n+m<<"\n";
    }
    return 0;
}