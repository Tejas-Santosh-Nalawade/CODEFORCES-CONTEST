#include <bits/stdc++.h>
using namespace std;

#define fastio() ios::sync_with_stdio(false); cin.tie(nullptr);
using ll=long long
#define endl '\n'

int main() {
    fastio();

    int tt;
    cin >> tt;
    while (tt--) {
        ll n;
        cin>>n;
        vector<int>a(n);
        for(auto &m: a){
            cin>>m;
        }

        string x; cin>>x;

        
    }
    return 0;
}