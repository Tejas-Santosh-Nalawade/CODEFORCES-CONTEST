#include <bits/stdc++.h>
using namespace std;

#define fastio() ios::sync_with_stdio(false); cin.tie(nullptr);
using ll = long long;
#define endl '\n'

void solve(){
    int n;
    cin >> n;
    vector<ll> a(n);
    for(auto &x : a) cin >> x;
    
}


int main() {
    fastio();

    int tt;
    cin >> tt;
    while (tt--) {
        solve();
    }
    return 0;
}